# Sample Workflows

This directory is reserved for sanitized example workflows.

The project does not bundle ComfyUI, model files, LoRA files, custom nodes, production workflows, private prompts, or generated outputs. Add only workflows that are safe to publish and do not contain private paths, client data, personal prompts, or model files with unclear redistribution terms.
